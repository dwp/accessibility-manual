{% set navItems = {
  sectionId: sectionId,
  pageId: pageId,
  navItems: navItems
} %}

<header role="banner" data-module="govuk-header">
  {{appHeader()}}
  {{appMobileNavigation(navItems)}}
  {{appPhaseBanner()}}
</header>
{{appNavigation(navItems)}}
