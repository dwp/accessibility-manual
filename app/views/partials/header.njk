{% import '../_globals/navigation-data.njk' as globals %}

{% set navItems = {
  sectionId: sectionId,
  pageId: pageId,
  navItems: globals.navItems
} %}

{{appHeader()}}
{{appMobileNavigation(navItems)}}
{{appPhaseBanner()}}
{{appNavigation(navItems)}}
